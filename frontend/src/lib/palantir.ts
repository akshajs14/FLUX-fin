// Palantir integration removed — predictions now run locally via localModel.ts
// This file is kept as a stub to prevent import errors from any remaining references.

export const auth = {
  signIn: async () => { /* no-op */ },
};

export const palantirClient = null;
export const MODEL_RID = '';
